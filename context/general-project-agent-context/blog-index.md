# Blog index pointer

Canonical blog index lives at `website/app/blog/page.tsx` (App Router). `web-publisher-publish`
creates it on the first publish and adds a new post card at the top of the grid on every
later one. Post pages live at `website/app/blog/{slug}/page.tsx`.
